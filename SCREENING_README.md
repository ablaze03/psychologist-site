# Психологический скрининг-тест

Профессиональный интерактивный скрининг-тест для оценки эмоционального состояния. Основан на признанных методиках PHQ-9, GAD-7, PSS-10, WHO-5.

## 🎯 Возможности

- **Две версии теста:**
  - Quick (2-3 минуты) - базовый скрининг
  - Deep (10-12 минут) - детальная оценка

- **Тематические модули:**
  - Отношения
  - Тревога и мысли
  - Работа и выгорание
  - Другое

- **Профессиональные шкалы:**
  - PHQ-2/PHQ-9 (депрессивная симптоматика)
  - GAD-2/GAD-7 (тревога)
  - PSS-SHORT/PSS-10 (стресс)
  - WHO-SHORT/WHO-5 (психологический ресурс)

- **Safety screen** - обязательная проверка суицидальных мыслей
- **Визуализация результатов** - интерактивный график по шкалам
- **Персонализированная интерпретация** - тексты с рекомендациями
- **Лид-форма** - сбор контактов для отправки расшифровки
- **Аналитика** - отслеживание всех событий теста

## 🏗️ Архитектура

### Структура файлов

```
src/
├── types/
│   └── screening.ts          # TypeScript типы
├── config/
│   ├── questions.ts          # Конфигурация вопросов
│   ├── resultTexts.ts        # Тексты интерпретаций
│   └── uiTexts.ts            # UI тексты
├── lib/
│   ├── scoring.ts            # Scoring engine
│   ├── testStateMachine.ts   # State management
│   ├── analytics.ts          # Аналитика
│   └── webhook.ts            # Webhook интеграции
├── components/screening/
│   ├── WelcomeScreen.astro
│   ├── TopicSelectScreen.astro
│   ├── DepthSelectScreen.astro
│   ├── QuestionScreen.astro
│   ├── SafetyScreen.astro
│   ├── ResultsScreen.astro
│   ├── ResultChart.astro
│   └── ProgressBar.astro
└── pages/
    └── screening.astro       # Главная страница теста
```

## 📝 Конфигурация

### Вопросы

Все вопросы конфигурируются в `src/config/questions.ts`:

```typescript
{
  id: 'phq2_1',
  text: 'Мало интереса или удовольствия от того, чем занимаетесь?',
  scale: 'PHQ2',
  evidence_source: 'PHQ-9',
  reverse_scored: false,
  answers: standardAnswers,
  version: 'both',
  topic_gate: 'all',
  order: 1
}
```

**Параметры:**
- `id` - уникальный идентификатор
- `text` - текст вопроса
- `scale` - шкала (PHQ2, GAD7, PSS10 и т.д.)
- `evidence_source` - источник методики
- `reverse_scored` - обратный подсчет (для WHO-5)
- `answers` - варианты ответов
- `version` - quick/deep/both
- `topic_gate` - для какой темы (relationships/anxiety/work/other/all)
- `order` - порядок показа

### Тексты интерпретации

Конфигурируются в `src/config/resultTexts.ts`:

```typescript
{
  scale: 'PHQ9',
  level: 'moderate',
  version: 'deep',
  title: 'Умеренные депрессивные симптомы',
  summary: 'Результаты указывают на умеренную депрессивную симптоматику...',
  recommendations: [
    'Запишитесь на консультацию психолога',
    'Ведите дневник настроения'
  ],
  therapy_prompts: [
    'Когда вы начали замечать снижение настроения?',
    'Что помогает чувствовать себя лучше?'
  ]
}
```

**Уровни выраженности:**
- `low` (0-33%) - низкая/норма
- `moderate` (34-66%) - умеренная
- `high` (67-100%) - выраженная

### UI тексты

Все UI тексты в `src/config/uiTexts.ts` можно легко изменить для локализации или редактирования формулировок.

## 🔧 Scoring Engine

### Алгоритм подсчета

1. **Базовый подсчет:**
   ```
   score = answer (0, 1, 2, 3)
   ```

2. **Обратный подсчет (reverse_scored):**
   ```
   score = 3 - answer
   ```

3. **Процент по шкале:**
   ```
   percent = (sum_score / max_score) * 100
   ```

4. **Уровень выраженности:**
   - 0-33% → low
   - 34-66% → moderate
   - 67-100% → high

### Функции

```typescript
// Вычислить баллы
computeScores(responses, topic, version)

// Получить топ-2 зоны беспокойства
getTopConcerns(scores)

// Проверить safety триггер
checkSafetyTrigger(responses)

// Получить вопросы для сессии
getQuestionsForSession(topic, version)
```

## 🎨 Кастомизация стилей

CSS переменные в `screening.astro`:

```css
:root {
  --accent-primary: #7c3aed;
  --accent-primary-hover: #6d28d9;
  --accent-secondary: #a78bfa;
  --accent-light: #f5f3ff;
  
  --text-primary: #1f2937;
  --text-secondary: #6b7280;
  
  --bg-primary: #ffffff;
  --bg-secondary: #f9fafb;
  
  --border-light: #e5e7eb;
}
```

## 🔌 Интеграции

### Environment Variables

Создайте `.env` файл:

```bash
# Webhook для отправки лидов
PUBLIC_WEBHOOK_URL=https://your-webhook-url.com

# Webhook для отправки email
PUBLIC_EMAIL_WEBHOOK_URL=https://your-email-webhook.com

# Yandex Metrika ID (опционально)
PUBLIC_YM_ID=12345678
```

### Аналитика

Поддерживается:
- Google Analytics 4 (gtag)
- Meta Pixel (fbq)
- Yandex Metrika (ym)

События:
- `test_start`
- `topic_selected`
- `version_selected`
- `question_answered`
- `safety_triggered`
- `results_view`
- `lead_submit`
- `booking_click`

### Webhook

Формат отправляемых данных:

```json
{
  "session_id": "1234567890-abc123",
  "topic": "relationships",
  "version": "deep",
  "scores": {
    "PHQ9": 45,
    "GAD7": 60,
    "PSS10": 55,
    "WHO5": 40,
    "TOPIC_REL": 70
  },
  "top_concerns": ["Отношения", "Тревожные симптомы"],
  "contact_email": "user@example.com",
  "contact_telegram": "@username",
  "raw_answers": { ... } // если включен флаг send_raw_answers
}
```

## 🚀 Развертывание

1. **Установка зависимостей:**
   ```bash
   npm install
   ```

2. **Локальная разработка:**
   ```bash
   npm run dev
   ```

3. **Сборка:**
   ```bash
   npm run build
   ```

4. **Деплой:**
   ```bash
   ./deploy.sh
   ```

## 📊 Использование

1. Откройте `/screening` на вашем сайте
2. Пользователь проходит тест
3. Получает результаты с визуализацией
4. Оставляет контакты для расшифровки
5. Данные отправляются на webhook

## 🔒 Безопасность и Этика

- **Дисклеймер** показывается на первом экране
- **Не является диагнозом** - указывается в результатах
- **Safety screen** - обязательная проверка суицидальных мыслей
- **Персональные данные** - сохраняются только с согласия
- **Прозрачность методик** - указываются источники шкал

## 🛠️ Добавление новых вопросов

1. Откройте `src/config/questions.ts`
2. Добавьте новый объект в массив `questions`:

```typescript
{
  id: 'custom_1',
  text: 'Ваш вопрос?',
  scale: 'TOPIC_REL', // или другая шкала
  evidence_source: 'Custom',
  reverse_scored: false,
  answers: standardAnswers,
  version: 'deep',
  topic_gate: 'relationships',
  order: 200
}
```

3. При необходимости добавьте интерпретацию в `src/config/resultTexts.ts`

## 🎯 Лучшие практики

1. **Не меняйте PHQ/GAD/PSS/WHO вопросы** - они валидированы исследованиями
2. **Кастомные вопросы** - добавляйте в тематические модули
3. **Тестируйте изменения** - особенно scoring logic
4. **Мониторьте Safety триггеры** - критически важно для безопасности
5. **Регулярно обновляйте тексты** - на основе обратной связи

## 📞 Поддержка

При возникновении вопросов или проблем:
- Проверьте логи в консоли браузера
- Убедитесь, что все env переменные настроены
- Проверьте работу webhook endpoints

## 📄 Лицензия

Код теста распространяется с открытым исходным кодом.
Методики PHQ-9, GAD-7, PSS-10, WHO-5 являются общедоступными инструментами.
