# ✅ Реализована отправка результатов теста

## 📦 Что добавлено

### 1. Модуль отправки результатов
**Файл:** `site/src/config/webhook.ts`

Три функции отправки:
- `sendToTelegram()` — отправка в Telegram Bot
- `sendToEmail()` — отправка на Email через FormSubmit
- `sendTestResults()` — универсальная (пробует все методы)

### 2. Обновлена форма отправки
**Файл:** `site/src/pages/test.astro`

- ✅ Реальная отправка данных (не только console.log)
- ✅ Индикация "Отправка..." на кнопке
- ✅ Обработка ошибок
- ✅ Блокировка повторной отправки

### 3. Документация

| Файл | Описание |
|------|----------|
| `НАСТРОЙКА_ОТПРАВКИ.md` | Полная инструкция по настройке (Telegram + Email) |
| `БЫСТРАЯ_НАСТРОЙКА_TELEGRAM.md` | Быстрый старт за 2 минуты |
| `env.example` | Пример файла с переменными окружения |

---

## 🤖 Формат сообщения в Telegram

Пример того, что вы будете получать:

```
🆕 Новый результат теста

👤 Контакт клиента:
📧 kate_sp3@example.com

📊 Результаты:
🟦 Депрессия (PHQ-9): 12 баллов — умеренный
🟧 Тревога (GAD-7): 8 баллов — лёгкий

Дата: 12.01.2026, 15:30
```

### При тяжёлых состояниях добавляется:

```
🔴 Тяжёлое состояние — рекомендован психотерапевт/психиатр
```

### При срабатывании safety-триггера:

```
⚠️ ВНИМАНИЕ: Сработал safety-триггер (суицидальные мысли)
```

---

## 🚀 Как настроить?

### Вариант 1: Telegram (рекомендуется) — 2 минуты

1. **@BotFather** → `/newbot` → скопируйте токен
2. **@userinfobot** → скопируйте Chat ID
3. Откройте `site/src/config/webhook.ts`
4. Вставьте токен и Chat ID
5. Перезапустите сервер: `npm run dev`

Подробно: `site/src/screening/БЫСТРАЯ_НАСТРОЙКА_TELEGRAM.md`

### Вариант 2: Email — 1 минута

1. Откройте `site/src/config/webhook.ts`
2. Укажите свой email в `recipientEmail`
3. Включите: `enabled: true`
4. Подтвердите email при первой отправке

### Вариант 3: Оба метода

Включите `enabled: true` для обоих — результаты будут дублироваться.

---

## 🔐 Безопасность

### Хранение секретов

Вместо хардкода в `webhook.ts`, используйте `.env`:

```bash
# Создайте файл .env в корне проекта
PUBLIC_TELEGRAM_BOT_TOKEN=ваш_токен
PUBLIC_TELEGRAM_CHAT_ID=ваш_chat_id
```

Файл `.env` уже в `.gitignore` — не попадёт в репозиторий.

### Для продакшена

На хостинге (Netlify/Vercel) добавьте переменные через UI:
- Settings → Environment Variables
- Добавьте `PUBLIC_TELEGRAM_BOT_TOKEN` и `PUBLIC_TELEGRAM_CHAT_ID`

---

## 📊 Что отправляется?

Клиенту:
- Ничего не отправляется автоматически (только форма "Отправить мне результаты")

Вам (психологу):
- 👤 Контакт клиента (email или telegram)
- 📊 Баллы по PHQ-9 и GAD-7
- 🎯 Уровень (минимальный/лёгкий/умеренный/тяжёлый)
- ⚠️ Safety-триггер (если был)
- 📅 Дата и время прохождения

**НЕ отправляются:**
- Ответы на каждый вопрос (слишком много данных)
- IP-адрес
- Другие персональные данные

---

## 🧪 Тестирование

### 1. Локально

```bash
npm run dev
```

Откройте `http://localhost:4321/test` и пройдите тест.

### 2. Проверьте консоль

Откройте DevTools (F12) → Console.  
Должно быть: `Результат отправки: { success: true, results: [...] }`

### 3. Проверьте Telegram/Email

Вам должно прийти сообщение с результатами.

---

## ❌ Решение проблем

### Ошибка: "Telegram не настроен"

- Проверьте `webhook.ts` — заполнены ли `botToken` и `chatId`
- Проверьте `enabled: true`

### Сообщения не приходят

1. ✅ Нажали Start в боте?
2. ✅ Правильный токен/chat_id?
3. ✅ Нет лишних пробелов?
4. ✅ Консоль браузера (F12) — есть ошибка?

### FormSubmit: email не приходит

1. ✅ Проверьте папку Спам
2. ✅ Подтвердите email через первое письмо

---

## 📈 Что дальше?

### Возможные улучшения:

1. **База данных** — сохранять все результаты
2. **Dashboard** — статистика по всем клиентам
3. **Webhook в CRM** — автоматическое создание карточки клиента
4. **PDF-отчёт** — красивый отчёт для клиента
5. **Email-рассылка** — автоматическое письмо клиенту с рекомендациями

---

## 📁 Изменённые/созданные файлы

```
site/
├── src/
│   ├── config/
│   │   └── webhook.ts                          ← НОВЫЙ: модуль отправки
│   ├── pages/
│   │   └── test.astro                          ← ОБНОВЛЁН: форма
│   └── screening/
│       ├── НАСТРОЙКА_ОТПРАВКИ.md               ← НОВЫЙ: полная инструкция
│       ├── БЫСТРАЯ_НАСТРОЙКА_TELEGRAM.md       ← НОВЫЙ: быстрый старт
│       └── ЧТО_СДЕЛАНО_ОТПРАВКА.md            ← НОВЫЙ: этот файл
└── env.example                                  ← НОВЫЙ: пример .env
```

---

## ✅ Готово к использованию!

1. 📖 Прочитайте `БЫСТРАЯ_НАСТРОЙКА_TELEGRAM.md`
2. ⚙️ Настройте токен и chat_id
3. 🧪 Протестируйте на `localhost:4321/test`
4. 🚀 Деплойте на продакшен

---

**Автор:** Claude (Anthropic)  
**Дата:** 12 января 2026  
**Статус:** ✅ Готово к использованию
