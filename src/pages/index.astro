---
import Layout from '../layouts/Layout.astro';
import Navigation from '../components/Navigation.astro';
import FAQ from '../components/FAQ.astro';
import Divider from '../components/Divider.astro';
import HighlightMarker from '../components/HighlightMarker.astro';
import ProcessPath from '../components/ProcessPath.astro';
import ScreeningModal from '../components/ScreeningModal.astro';
import contentData from '../../content.json';
import type { ContentData } from '../types';

const content = contentData as ContentData;

// Создаём ссылку на Telegram с prefilled текстом
const telegramLinkWithMessage = `https://t.me/Irina_Spva?text=${encodeURIComponent(content.contacts.messageTemplate)}`;
---

<Layout content={content}>
  <Navigation navItems={content.navigation.items} />
  
  <!-- Hero -->
  <section id="hero" class="section min-h-[60vh] md:min-h-[75vh] flex items-center bg-base relative overflow-hidden">
    <!-- Мягкая подсветка за текстом -->
    <div class="absolute left-0 top-[50%] w-64 h-64 md:w-96 md:h-96 bg-peach/20 rounded-full opacity-40 blur-3xl -z-0 hero-blob-left"></div>
    <!-- Декоративный блоб за фото -->
    <div class="absolute right-0 top-[50%] w-64 h-64 md:w-96 md:h-96 bg-ice/30 rounded-full opacity-30 blur-3xl -z-0 hero-blob-right"></div>
    
    <div class="container-custom relative z-10">
      <div class="grid md:grid-cols-2 gap-6 md:gap-10 items-center">
        <div class="order-1 md:order-1">
          <h1 class="heading-1 mb-6 text-ink">
            Психотерапия, в которой можно <HighlightMarker>выдохнуть</HighlightMarker>
          </h1>
          <p class="hero-subtitle text-ink/80 mb-4 font-light">
            Помогаю разобраться с тревогой, отношениями и самооценкой <HighlightMarker>бережно</HighlightMarker>, <HighlightMarker>в вашем темпе</HighlightMarker>.<br />
            <span class="hero-subtitle-small">Онлайн и очно в г. Москва.</span>
          </p>
          <p class="hero-subtitle-small text-ink/60 mb-6 leading-relaxed">
            {content.hero.microText}
          </p>
          <div class="flex flex-col sm:flex-row gap-4">
            <a href={content.hero.cta.primary.link} class="btn-primary">
              {content.hero.cta.primary.text}
            </a>
            <a href={content.hero.cta.secondary.link} class="btn-secondary">
              {content.hero.cta.secondary.text}
            </a>
          </div>
          
          <!-- Кнопка открытия теста -->
          <div class="mt-6 flex items-center gap-3">
            <button 
              data-screening-trigger 
              class="screening-trigger-btn"
              aria-label="Пройти мини чек-ап состояния"
            >
              <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
                <path d="M9 11l3 3L22 4"></path>
                <path d="M21 12v7a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2V5a2 2 0 0 1 2-2h11"></path>
              </svg>
              <span>Пройти мини чек-ап состояния</span>
              <span class="screening-badge">5 мин</span>
            </button>
          </div>
        </div>
        <div class="order-2 md:order-2 relative z-10">
          <img 
            src="/images/portrait.jpg"
            alt="Спиридонова Ирина — клинический психолог"
            class="hero-image w-full mx-auto rounded-2xl border border-nimbus/40 shadow-lg object-cover aspect-[3/4]"
            loading="eager"
          />
        </div>
      </div>
    </div>
  </section>
  
  <style>
    .hero-subtitle {
      font-size: clamp(1.125rem, 1.4vw, 1.5rem);
      line-height: 1.5;
    }
    
    .hero-subtitle-small {
      font-size: clamp(1rem, 1.2vw, 1.25rem);
    }
    
    .hero-image {
      max-width: min(20rem, 100%);
    }
    
    @media (min-width: 768px) {
      .hero-image {
        max-width: min(28rem, 100%);
      }
    }
    
    @media (min-width: 1280px) {
      .hero-image {
        max-width: min(24rem, 100%);
      }
    }
    
    @media (min-width: 1440px) {
      .hero-image {
        max-width: 28rem;
      }
    }
    
    .hero-blob-left,
    .hero-blob-right {
      margin-top: -12rem;
    }
  </style>
  
  <Divider color="ice" position="bottom" />

  <!-- Кому подходит -->
  <section id="suitable-for" class="section bg-ice">
    <div class="container-custom">
      <div class="max-w-4xl mx-auto">
        <h2 class="heading-2 mb-4 text-ink">
          <HighlightMarker>{content.suitableFor.title}</HighlightMarker>
        </h2>
        <ul class="space-y-3 mb-4 text-lg md:text-xl text-ink/80 leading-relaxed">
          <li class="flex items-start">
            <span class="text-ink/40 mr-3">•</span>
            <span>Мало <span class="underline-green">энергии</span>: даже привычные дела даются тяжелее</span>
          </li>
          <li class="flex items-start">
            <span class="text-ink/40 mr-3">•</span>
            <span>Трудно <span class="underline-green">отключиться от мыслей</span>: они крутятся по кругу, отдых не восстанавливает</span>
          </li>
          <li class="flex items-start">
            <span class="text-ink/40 mr-3">•</span>
            <span>В <span class="underline-green">отношениях</span> много <span class="underline-green">напряжения</span> или повторяются похожие сценарии</span>
          </li>
          <li class="flex items-start">
            <span class="text-ink/40 mr-3">•</span>
            <span>Сложно <span class="underline-green">говорить «нет»</span> и выбирать себя без чувства вины</span>
          </li>
          <li class="flex items-start">
            <span class="text-ink/40 mr-3">•</span>
            <span><span class="underline-green">Самооценка</span> «качает»: то уверенность, то сомнения и самокритика</span>
          </li>
          <li class="flex items-start">
            <span class="text-ink/40 mr-3">•</span>
            <span>Хочется <span class="underline-green">ясности</span>: понять себя, свои желания и куда дальше</span>
          </li>
          <li class="flex items-start">
            <span class="text-ink/40 mr-3">•</span>
            <span>Сейчас <span class="underline-green">непростой период</span> (перемены, разрыв, утрата) и нужна опора</span>
          </li>
        </ul>
        <p class="text-lg font-medium text-ink mb-5">Вы можете начать терапию с того, в чём сейчас <span class="underline-green">тяжелее всего</span>.</p>
        <div class="flex flex-wrap gap-3">
          {content.suitableFor.chips.map((chip) => (
            <span class="chip">{chip}</span>
          ))}
        </div>
      </div>
    </div>
  </section>
  
  <style>
    .underline-green {
      text-decoration: underline;
      text-decoration-color: var(--color-aquaSoft);
      text-decoration-thickness: 2px;
      text-underline-offset: 3px;
    }
  </style>
  
  <Divider color="base" position="bottom" />

  <!-- С чем работаю -->
  <section id="services" class="section bg-base">
    <div class="container-custom">
      <div class="max-w-4xl mx-auto">
        <h2 class="heading-2 mb-8 text-center text-ink">
          <HighlightMarker>{content.services.title}</HighlightMarker>
        </h2>
        <div class="grid md:grid-cols-2 gap-6 mb-6">
          {content.services.items.map((item) => (
            <div class="card-service">
              <h3 class="text-xl font-medium text-ink mb-2">{item.title}</h3>
              <p class="text-ink/70 leading-relaxed">{item.description}</p>
            </div>
          ))}
        </div>
        <p class="text-center text-ink/70 max-w-3xl mx-auto">{content.services.finalNote}.</p>
      </div>
    </div>
  </section>
  
  <Divider color="peach" position="bottom" />

  <!-- Путь терапии -->
  <ProcessPath process={content.process} />
  
  <Divider color="base" position="bottom" />

  <!-- Обо мне -->
  <section id="about" class="section bg-aquaSoft">
    <div class="container-custom">
      <div class="max-w-6xl mx-auto">
        <!-- Фото и основная информация -->
        <div class="grid md:grid-cols-[2fr,3fr] gap-6 md:gap-8 items-start mb-6">
          <!-- Фото слева -->
          <div class="order-2 md:order-1">
            <img 
              src="/images/about.jpg"
              alt="Ирина Спиридонова"
              class="w-full rounded-2xl shadow-lg object-cover aspect-[3/4]"
              loading="lazy"
            />
          </div>
          
          <!-- Информация справа -->
          <div class="order-1 md:order-2 card bg-base/90 !p-5 md:!p-6">
            <h2 class="heading-2 mb-0.5 text-ink">
              {content.about.name}
            </h2>
            <p class="text-lg text-ink/60 mb-3">{content.about.role}</p>
            
            <p class="text-base text-ink/80 leading-relaxed mb-4">
              {content.about.description}
            </p>
            
            <!-- Факты в один столбец -->
            <div class="space-y-3">
              {content.about.experience.map((item) => (
                <div class="border-l-2 border-aquaSoft/30 pl-4">
                  <p class="font-medium text-ink mb-0.5">{item.title}</p>
                  <p class="text-sm text-ink/70 leading-relaxed">{item.description}</p>
                </div>
              ))}
            </div>
          </div>
        </div>
        
        <!-- Как я работаю и Моя философия на всю ширину -->
        <div class="card bg-base/90 !p-5 md:!p-6">
          <div class="grid md:grid-cols-2 gap-6">
            <!-- Как я работаю -->
            <div class="border-l-2 border-aquaSoft/30 pl-4">
              <p class="font-semibold text-base text-ink mb-2">{content.about.approach.title}</p>
              <p class="text-sm text-ink/80 leading-relaxed">{content.about.approach.text}</p>
            </div>
            
            <!-- Моя философия -->
            <div class="border-l-2 border-peach/30 pl-4">
              <p class="font-semibold text-base text-ink mb-2">{content.about.philosophy.title}</p>
              <p class="text-sm text-ink/80 leading-relaxed">{content.about.philosophy.text}</p>
            </div>
          </div>
        </div>
        
        <!-- Дипломы и сертификаты -->
        <div class="mt-8">
          <h3 class="heading-3 mb-3 text-center text-ink/80">
            Дипломы и сертификаты
          </h3>
          <p class="text-base text-ink/70 text-center mb-4 max-w-2xl mx-auto">
            Профессиональное развитие и обучение — важная часть моей работы. Здесь представлены сертификаты о прохождении курсов, тренингов и программ повышения квалификации, которые помогают мне быть лучшим специалистом для моих клиентов.
          </p>
          <div class="relative certificates-container">
            <!-- Стрелка влево -->
            <button 
              class="scroll-arrow scroll-arrow-left absolute left-0 top-1/2 -translate-y-1/2 z-10 bg-white/90 hover:bg-aquaSoft text-ink hover:text-white rounded-full p-3 shadow-lg transition-all duration-300 opacity-0 pointer-events-none"
              aria-label="Прокрутить влево"
            >
              <svg class="w-6 h-6" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M15 19l-7-7 7-7" />
              </svg>
            </button>
            
            <!-- Стрелка вправо -->
            <button 
              class="scroll-arrow scroll-arrow-right absolute right-0 top-1/2 -translate-y-1/2 z-10 bg-white/90 hover:bg-aquaSoft text-ink hover:text-white rounded-full p-3 shadow-lg transition-all duration-300 animate-pulse-slow"
              aria-label="Прокрутить вправо"
            >
              <svg class="w-6 h-6" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 5l7 7-7 7" />
              </svg>
            </button>
            
            <div class="overflow-x-auto pb-4 scrollbar-hide certificates-scroll" style="scroll-snap-type: x mandatory; scroll-behavior: smooth;">
              <div class="flex gap-4" style="scroll-snap-align: start;">
              {[
                '1.jpeg', '3.jpeg', '4.jpeg', 
                '5.jpeg', '6.jpeg', '7.jpeg', '8.jpeg',
                '9.jpeg', '10.jpeg', '11.jpeg', '12.jpeg'
              ].map((filename, i) => (
                <div 
                  class="flex-shrink-0 w-48 aspect-[3/4] bg-base/60 rounded-xl border border-nimbus/40 cursor-pointer transition-all hover:scale-[1.08] hover:shadow-lg certificate-item overflow-hidden"
                  data-index={i}
                  style="scroll-snap-align: start;"
                >
                  <img 
                    src={`/images/certificates/${filename}`}
                    alt={`Диплом или сертификат ${i + 1}`}
                    class="certificate-image w-full h-full object-cover"
                    style="object-position: center 25%;"
                    loading="lazy"
                  />
                </div>
              ))}
              <!-- Кнопка "Посмотреть все" -->
              <a 
                href="/certificates"
                class="flex-shrink-0 w-48 aspect-[3/4] bg-aquaSoft/30 rounded-xl border-2 border-dashed border-aquaSoft hover:bg-aquaSoft/50 transition-all flex flex-col items-center justify-center gap-3 group"
                style="scroll-snap-align: start;"
              >
                <svg class="w-12 h-12 text-aquaSoft group-hover:scale-110 transition-transform" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                  <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 4v16m8-8H4" />
                </svg>
                <div class="text-center px-4">
                  <p class="text-sm font-medium text-ink">Посмотреть все</p>
                  <p class="text-xs text-ink/60 mt-1">57 обучений</p>
                </div>
              </a>
              </div>
            </div>
          </div>
        </div>
        
        <script>
          document.addEventListener('DOMContentLoaded', () => {
            const scrollContainer = document.querySelector('.certificates-scroll');
            const leftArrow = document.querySelector('.scroll-arrow-left');
            const rightArrow = document.querySelector('.scroll-arrow-right');
            
            if (!scrollContainer || !leftArrow || !rightArrow) return;
            
            // Функция обновления видимости стрелок
            const updateArrows = () => {
              const scrollLeft = scrollContainer.scrollLeft;
              const maxScroll = scrollContainer.scrollWidth - scrollContainer.clientWidth;
              
              // Левая стрелка: показывать, если можем прокрутить влево
              if (scrollLeft > 10) {
                leftArrow.style.opacity = '1';
                leftArrow.style.pointerEvents = 'auto';
              } else {
                leftArrow.style.opacity = '0';
                leftArrow.style.pointerEvents = 'none';
              }
              
              // Правая стрелка: показывать, если можем прокрутить вправо
              if (scrollLeft < maxScroll - 10) {
                rightArrow.style.opacity = '1';
                rightArrow.style.pointerEvents = 'auto';
              } else {
                rightArrow.style.opacity = '0';
                rightArrow.style.pointerEvents = 'none';
              }
            };
            
            // Прокрутка влево
            leftArrow.addEventListener('click', () => {
              scrollContainer.scrollBy({ left: -300, behavior: 'smooth' });
            });
            
            // Прокрутка вправо
            rightArrow.addEventListener('click', () => {
              scrollContainer.scrollBy({ left: 300, behavior: 'smooth' });
            });
            
            // Обновляем стрелки при прокрутке
            scrollContainer.addEventListener('scroll', updateArrows);
            
            // Начальное обновление
            updateArrows();
          });
        </script>
        
        <style>
          @keyframes pulse-slow {
            0%, 100% {
              transform: scale(1);
              box-shadow: 0 10px 15px -3px rgba(0, 0, 0, 0.1);
            }
            50% {
              transform: scale(1.05);
              box-shadow: 0 20px 25px -5px rgba(0, 0, 0, 0.15);
            }
          }
          
          .animate-pulse-slow {
            animation: pulse-slow 2s ease-in-out infinite;
          }
          
          .scroll-arrow {
            backdrop-filter: blur(8px);
          }
          
          .scroll-arrow:hover {
            transform: translateY(-50%) scale(1.1);
          }
          
          .scroll-arrow:active {
            transform: translateY(-50%) scale(0.95);
          }
        </style>
        
        <!-- Lightbox Modal -->
        <div id="lightbox" class="fixed inset-0 bg-ink/90 z-50 hidden items-center justify-center p-4">
          <button id="lightbox-close" class="absolute top-4 right-4 text-base text-white hover:text-peach transition-colors" aria-label="Закрыть">
            <svg class="w-8 h-8" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M6 18L18 6M6 6l12 12" />
            </svg>
          </button>
          <img id="lightbox-image" src="" alt="Диплом или сертификат" class="max-w-full max-h-[90vh] rounded-lg object-contain" />
        </div>
        
        <script>
          document.addEventListener('DOMContentLoaded', () => {
            const lightbox = document.getElementById('lightbox');
            const lightboxImage = document.getElementById('lightbox-image');
            const lightboxClose = document.getElementById('lightbox-close');
            const certificates = document.querySelectorAll('.certificate-item');
            
            certificates.forEach(cert => {
              cert.addEventListener('click', () => {
                const img = cert.querySelector('.certificate-image');
                const imgSrc = img?.src || '';
                if (imgSrc && imgSrc !== '') {
                  lightboxImage.src = imgSrc;
                  lightboxImage.alt = img?.alt || 'Диплом или сертификат';
                  lightbox.classList.remove('hidden');
                  lightbox.classList.add('flex');
                  document.body.style.overflow = 'hidden';
                }
              });
            });
            
            const closeLightbox = () => {
              lightbox.classList.add('hidden');
              lightbox.classList.remove('flex');
              document.body.style.overflow = '';
            };
            
            lightboxClose.addEventListener('click', closeLightbox);
            lightbox.addEventListener('click', (e) => {
              if (e.target === lightbox) closeLightbox();
            });
            
            document.addEventListener('keydown', (e) => {
              if (e.key === 'Escape' && !lightbox.classList.contains('hidden')) {
                closeLightbox();
              }
            });
          });
        </script>
        
        <style>
          .scrollbar-hide {
            -ms-overflow-style: none;
            scrollbar-width: none;
          }
          .scrollbar-hide::-webkit-scrollbar {
            display: none;
          }
        </style>
      </div>
    </div>
  </section>
  
  <Divider color="base" position="bottom" />

  <!-- Как я работаю -->
  <section id="approach" class="section bg-aquaSoft/10">
    <div class="container-custom">
      <div class="max-w-5xl mx-auto">
        <h2 class="heading-2 mb-8 text-center text-ink">
          {content.approach.title}
        </h2>
        
        <div class="grid md:grid-cols-3 gap-6">
          {content.approach.principles.map((principle) => (
            <div class="bg-base/90 rounded-2xl p-6 border border-aquaSoft/30 shadow-sm hover:shadow-md transition-all duration-300">
              <div class="flex items-start gap-3 mb-3">
                <span class="inline-flex items-center px-3 py-1 rounded-full text-xs font-medium bg-aquaSoft/30 text-ink border border-aquaSoft/50">
                  {principle.number}
                </span>
              </div>
              <h3 class="text-xl font-medium text-ink mb-3">{principle.title}</h3>
              <p class="text-base text-ink/70 leading-relaxed">{principle.description}</p>
            </div>
          ))}
        </div>
      </div>
    </div>
  </section>
  
  <Divider color="base" position="bottom" />

  <!-- Формат и стоимость -->
  <section id="pricing" class="section bg-base">
    <div class="container-custom">
      <div class="max-w-5xl mx-auto">
        <h2 class="heading-2 mb-3 text-center text-ink">
          <span class="relative inline-block"><span class="absolute bottom-0 left-0 right-0 h-0.5 bg-ice/50 -z-10"></span>{content.pricing.title}</span>
        </h2>
        <p class="text-lg text-ink/70 mb-10 text-center">
          {content.pricing.subtitle}
        </p>
        
        <div class="grid md:grid-cols-2 gap-6 mb-6">
          {content.pricing.formats.map((format, index) => (
            <div class={`relative bg-gradient-to-br ${index === 0 ? 'from-ice/80 to-ice/40' : 'from-aquaSoft/20 to-ice/40'} rounded-2xl p-6 border-2 ${index === 0 ? 'border-aquaSoft/30' : 'border-aquaSoft/40'} shadow-lg hover:shadow-xl transition-all duration-300 group`}>
              {/* Значок формата */}
              <div class="inline-flex items-center justify-center w-14 h-14 rounded-full bg-white/80 mb-3 group-hover:scale-110 transition-transform">
                {index === 0 ? (
                  <svg class="w-7 h-7 text-aquaSoft" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9.75 17L9 20l-1 1h8l-1-1-.75-3M3 13h18M5 17h14a2 2 0 002-2V5a2 2 0 00-2-2H5a2 2 0 00-2 2v10a2 2 0 002 2z" />
                  </svg>
                ) : (
                  <svg class="w-7 h-7 text-aquaSoft" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M17 20h5v-2a3 3 0 00-5.356-1.857M17 20H7m10 0v-2c0-.656-.126-1.283-.356-1.857M7 20H2v-2a3 3 0 015.356-1.857M7 20v-2c0-.656.126-1.283.356-1.857m0 0a5.002 5.002 0 019.288 0M15 7a3 3 0 11-6 0 3 3 0 016 0zm6 3a2 2 0 11-4 0 2 2 0 014 0zM7 10a2 2 0 11-4 0 2 2 0 014 0z" />
                  </svg>
                )}
              </div>
              
              {/* Заголовок */}
              <h3 class="text-2xl font-semibold text-ink mb-2">{format.title}</h3>
              
              {/* Цена */}
              <div class="mb-4">
                <span class="text-4xl font-bold text-ink">{format.price}</span>
                <span class="text-ink/60 ml-2">/ {format.duration}</span>
              </div>
              
              {/* Детали */}
              <div class="space-y-2 mb-3">
                {/* Продолжительность */}
                <div class="flex items-start gap-3">
                  <svg class="w-5 h-5 text-aquaSoft flex-shrink-0 mt-0.5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 8v4l3 3m6-3a9 9 0 11-18 0 9 9 0 0118 0z" />
                  </svg>
                  <div>
                    <p class="text-sm font-medium text-ink">Длительность</p>
                    <p class="text-sm text-ink/70">{format.duration}</p>
                  </div>
                </div>
                
                {/* Платформа или Место */}
                {format.platform && (
                  <div class="flex items-start gap-3">
                    <svg class="w-5 h-5 text-aquaSoft flex-shrink-0 mt-0.5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                      <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M21 12a9 9 0 01-9 9m9-9a9 9 0 00-9-9m9 9H3m9 9a9 9 0 01-9-9m9 9c1.657 0 3-4.03 3-9s-1.343-9-3-9m0 18c-1.657 0-3-4.03-3-9s1.343-9 3-9m-9 9a9 9 0 019-9" />
                    </svg>
                    <div>
                      <p class="text-sm font-medium text-ink">Платформа</p>
                      <p class="text-sm text-ink/70">{format.platform}</p>
                    </div>
                  </div>
                )}
                
                {format.location && (
                  <div class="flex items-start gap-3">
                    <svg class="w-5 h-5 text-aquaSoft flex-shrink-0 mt-0.5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                      <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M17.657 16.657L13.414 20.9a1.998 1.998 0 01-2.827 0l-4.244-4.243a8 8 0 1111.314 0z" />
                      <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M15 11a3 3 0 11-6 0 3 3 0 016 0z" />
                    </svg>
                    <div>
                      <p class="text-sm font-medium text-ink">{format.location}</p>
                      <p class="text-xs text-ink/60">{format.locationNote}</p>
                    </div>
                  </div>
                )}
                
                {/* Отмена/перенос */}
                <div class="flex items-start gap-3">
                  <svg class="w-5 h-5 text-aquaSoft flex-shrink-0 mt-0.5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M8 7V3m8 4V3m-9 8h10M5 21h14a2 2 0 002-2V7a2 2 0 00-2-2H5a2 2 0 00-2 2v12a2 2 0 002 2z" />
                  </svg>
                  <div>
                    <p class="text-sm font-medium text-ink">Условия переноса</p>
                    <p class="text-sm text-ink/70">{format.cancellation}</p>
                  </div>
                </div>
              </div>
              
              {/* Примечание */}
              <div class="pt-4 border-t border-ink/10">
                <p class="text-sm text-ink/60 italic">{format.note}</p>
              </div>
            </div>
          ))}
        </div>
        
        {/* Важная информация */}
        <div class="bg-peach/20 rounded-xl p-6 mb-5 border border-peach/30">
          <div class="flex items-start gap-3">
            <svg class="w-6 h-6 text-peach flex-shrink-0 mt-0.5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M13 16h-1v-4h-1m1-4h.01M21 12a9 9 0 11-18 0 9 9 0 0118 0z" />
            </svg>
            <div>
              <p class="font-medium text-ink mb-1">Важно знать</p>
              <p class="text-ink/70">{content.pricing.paymentNote}</p>
            </div>
          </div>
        </div>
        
        <p class="text-center text-ink/60 text-base">
          {content.pricing.microText}
        </p>
      </div>
    </div>
  </section>

  <!-- FAQ -->
  <FAQ faq={content.faq} />
  
  <Divider color="peach" position="bottom" />

  <!-- Контакты -->
  <section id="contacts" class="section bg-peach">
    <div class="container-custom">
      <div class="max-w-3xl mx-auto text-center">
        <h2 class="heading-2 mb-4 text-ink">
          <span class="relative inline-block"><span class="absolute bottom-0 left-0 right-0 h-0.5 bg-peach/50 -z-10"></span>{content.contacts.title}</span>
        </h2>
        <p class="text-lg text-ink/80 mb-2 leading-relaxed">
          {content.contacts.description}
        </p>
        <p class="text-base text-ink/70 mb-6 leading-relaxed">
          {content.contacts.descriptionNote}
        </p>
        
        <div class="mb-4">
          <a 
            href={telegramLinkWithMessage}
            class="inline-flex items-center gap-3 bg-gradient-to-r from-[#0088cc] to-[#00a0dc] hover:from-[#006ba8] hover:to-[#0088cc] text-white font-semibold px-8 py-4 rounded-2xl shadow-2xl hover:shadow-[0_20px_50px_rgba(0,136,204,0.4)] transition-all duration-300 text-lg group transform hover:scale-105"
            target="_blank"
          >
            <svg class="w-6 h-6 group-hover:rotate-12 transition-transform duration-300" fill="currentColor" viewBox="0 0 24 24">
              <path d="M11.944 0A12 12 0 0 0 0 12a12 12 0 0 0 12 12 12 12 0 0 0 12-12A12 12 0 0 0 12 0a12 12 0 0 0-.056 0zm4.962 7.224c.1-.002.321.023.465.14a.506.506 0 0 1 .171.325c.016.093.036.306.02.472-.18 1.898-.962 6.502-1.36 8.627-.168.9-.499 1.201-.82 1.23-.696.065-1.225-.46-1.9-.902-1.056-.693-1.653-1.124-2.678-1.8-1.185-.78-.417-1.21.258-1.91.177-.184 3.247-2.977 3.307-3.23.007-.032.014-.15-.056-.212s-.174-.041-.249-.024c-.106.024-1.793 1.14-5.061 3.345-.48.33-.913.49-1.302.48-.428-.008-1.252-.241-1.865-.44-.752-.245-1.349-.374-1.297-.789.027-.216.325-.437.893-.663 3.498-1.524 5.83-2.529 6.998-3.014 3.332-1.386 4.025-1.627 4.476-1.635z"/>
            </svg>
            <span>{content.contacts.subtitle}</span>
          </a>
        </div>
        
        <p class="text-sm text-ink/60 mb-3">
          В шаблоне сообщения нужно указать только ваши контакты и удобное время
        </p>
        
        <p class="text-base text-ink/70 mb-10 font-medium">
          Или выберите любой другой удобный способ связи ниже ↓
        </p>
        
        <!-- Контакты и соцсети -->
        <div class="bg-base/50 rounded-2xl p-6 mb-6">
          <h3 class="text-xl font-medium text-ink mb-4">Связаться со мной</h3>
          
          <div class="grid sm:grid-cols-2 gap-6 text-left">
            <!-- Телефон -->
            <a 
              href={content.contacts.phoneLink}
              class="flex items-center gap-3 p-4 bg-white rounded-xl hover:shadow-md transition-all group"
            >
              <div class="flex-shrink-0 w-12 h-12 bg-aquaSoft/20 rounded-full flex items-center justify-center group-hover:bg-aquaSoft/30 transition-colors">
                <svg class="w-6 h-6 text-aquaSoft" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                  <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M3 5a2 2 0 012-2h3.28a1 1 0 01.948.684l1.498 4.493a1 1 0 01-.502 1.21l-2.257 1.13a11.042 11.042 0 005.516 5.516l1.13-2.257a1 1 0 011.21-.502l4.493 1.498a1 1 0 01.684.949V19a2 2 0 01-2 2h-1C9.716 21 3 14.284 3 6V5z" />
                </svg>
              </div>
              <div>
                <p class="text-sm text-ink/60">Телефон</p>
                <p class="font-medium text-ink">{content.contacts.phone}</p>
              </div>
            </a>
            
            <!-- Email -->
            <a 
              href={content.contacts.emailLink}
              class="flex items-center gap-3 p-4 bg-white rounded-xl hover:shadow-md transition-all group"
            >
              <div class="flex-shrink-0 w-12 h-12 bg-aquaSoft/20 rounded-full flex items-center justify-center group-hover:bg-aquaSoft/30 transition-colors">
                <svg class="w-6 h-6 text-aquaSoft" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                  <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M3 8l7.89 5.26a2 2 0 002.22 0L21 8M5 19h14a2 2 0 002-2V7a2 2 0 00-2-2H5a2 2 0 00-2 2v10a2 2 0 002 2z" />
                </svg>
              </div>
              <div>
                <p class="text-sm text-ink/60">Email</p>
                <p class="font-medium text-ink break-all">{content.contacts.email}</p>
              </div>
            </a>
            
            <!-- Telegram -->
            <a 
              href={content.contacts.telegramLink}
              class="flex items-center gap-3 p-4 bg-white rounded-xl hover:shadow-md transition-all group"
              target="_blank"
            >
              <div class="flex-shrink-0 w-12 h-12 bg-aquaSoft/20 rounded-full flex items-center justify-center group-hover:bg-aquaSoft/30 transition-colors">
                <svg class="w-6 h-6 text-aquaSoft" fill="currentColor" viewBox="0 0 24 24">
                  <path d="M11.944 0A12 12 0 0 0 0 12a12 12 0 0 0 12 12 12 12 0 0 0 12-12A12 12 0 0 0 12 0a12 12 0 0 0-.056 0zm4.962 7.224c.1-.002.321.023.465.14a.506.506 0 0 1 .171.325c.016.093.036.306.02.472-.18 1.898-.962 6.502-1.36 8.627-.168.9-.499 1.201-.82 1.23-.696.065-1.225-.46-1.9-.902-1.056-.693-1.653-1.124-2.678-1.8-1.185-.78-.417-1.21.258-1.91.177-.184 3.247-2.977 3.307-3.23.007-.032.014-.15-.056-.212s-.174-.041-.249-.024c-.106.024-1.793 1.14-5.061 3.345-.48.33-.913.49-1.302.48-.428-.008-1.252-.241-1.865-.44-.752-.245-1.349-.374-1.297-.789.027-.216.325-.437.893-.663 3.498-1.524 5.83-2.529 6.998-3.014 3.332-1.386 4.025-1.627 4.476-1.635z"/>
                </svg>
              </div>
              <div>
                <p class="text-sm text-ink/60">Telegram</p>
                <p class="font-medium text-ink">@Irina_Spva</p>
              </div>
            </a>
            
            <!-- Instagram -->
            <a 
              href={content.contacts.instagram.link}
              class="flex items-center gap-3 p-4 bg-white rounded-xl hover:shadow-md transition-all group"
              target="_blank"
            >
              <div class="flex-shrink-0 w-12 h-12 bg-aquaSoft/20 rounded-full flex items-center justify-center group-hover:bg-aquaSoft/30 transition-colors">
                <svg class="w-6 h-6 text-aquaSoft" fill="currentColor" viewBox="0 0 24 24">
                  <path d="M12 2.163c3.204 0 3.584.012 4.85.07 3.252.148 4.771 1.691 4.919 4.919.058 1.265.069 1.645.069 4.849 0 3.205-.012 3.584-.069 4.849-.149 3.225-1.664 4.771-4.919 4.919-1.266.058-1.644.07-4.85.07-3.204 0-3.584-.012-4.849-.07-3.26-.149-4.771-1.699-4.919-4.92-.058-1.265-.07-1.644-.07-4.849 0-3.204.013-3.583.07-4.849.149-3.227 1.664-4.771 4.919-4.919 1.266-.057 1.645-.069 4.849-.069zm0-2.163c-3.259 0-3.667.014-4.947.072-4.358.2-6.78 2.618-6.98 6.98-.059 1.281-.073 1.689-.073 4.948 0 3.259.014 3.668.072 4.948.2 4.358 2.618 6.78 6.98 6.98 1.281.058 1.689.072 4.948.072 3.259 0 3.668-.014 4.948-.072 4.354-.2 6.782-2.618 6.979-6.98.059-1.28.073-1.689.073-4.948 0-3.259-.014-3.667-.072-4.947-.196-4.354-2.617-6.78-6.979-6.98-1.281-.059-1.69-.073-4.949-.073zm0 5.838c-3.403 0-6.162 2.759-6.162 6.162s2.759 6.163 6.162 6.163 6.162-2.759 6.162-6.163c0-3.403-2.759-6.162-6.162-6.162zm0 10.162c-2.209 0-4-1.79-4-4 0-2.209 1.791-4 4-4s4 1.791 4 4c0 2.21-1.791 4-4 4zm6.406-11.845c-.796 0-1.441.645-1.441 1.44s.645 1.44 1.441 1.44c.795 0 1.439-.645 1.439-1.44s-.644-1.44-1.439-1.44z"/>
                </svg>
              </div>
              <div>
                <p class="text-sm text-ink/60">Instagram</p>
                <p class="font-medium text-ink">{content.contacts.instagram.username}</p>
              </div>
            </a>
          </div>
          
          <!-- Telegram-канал -->
          <div class="mt-6 pt-6 border-t border-nimbus/30">
            <a 
              href={content.contacts.channel.link}
              class="inline-flex items-center gap-2 text-ink hover:text-aquaSoft transition-colors"
              target="_blank"
            >
              <svg class="w-5 h-5" fill="currentColor" viewBox="0 0 24 24">
                <path d="M11.944 0A12 12 0 0 0 0 12a12 12 0 0 0 12 12 12 12 0 0 0 12-12A12 12 0 0 0 12 0a12 12 0 0 0-.056 0zm4.962 7.224c.1-.002.321.023.465.14a.506.506 0 0 1 .171.325c.016.093.036.306.02.472-.18 1.898-.962 6.502-1.36 8.627-.168.9-.499 1.201-.82 1.23-.696.065-1.225-.46-1.9-.902-1.056-.693-1.653-1.124-2.678-1.8-1.185-.78-.417-1.21.258-1.91.177-.184 3.247-2.977 3.307-3.23.007-.032.014-.15-.056-.212s-.174-.041-.249-.024c-.106.024-1.793 1.14-5.061 3.345-.48.33-.913.49-1.302.48-.428-.008-1.252-.241-1.865-.44-.752-.245-1.349-.374-1.297-.789.027-.216.325-.437.893-.663 3.498-1.524 5.83-2.529 6.998-3.014 3.332-1.386 4.025-1.627 4.476-1.635z"/>
              </svg>
              <span class="font-medium">Telegram-канал с материалами и заметками</span>
            </a>
          </div>
        </div>
        
        <p class="text-sm text-ink/60">
          {content.contacts.disclaimer}
        </p>
      </div>
    </div>
  </section>

  <!-- Footer -->
  <footer class="bg-[#F3F4F6] py-10 border-t border-[#E5E7EB]">
    <div class="container-custom">
      <div class="max-w-3xl mx-auto space-y-6 text-sm">
        <div>
          <h3 class="text-sm mb-3 font-medium text-ink">{content.disclaimer.title}</h3>
          <p class="leading-relaxed whitespace-pre-line text-[#6B7280]">{content.disclaimer.content}</p>
        </div>
        
        <!-- Контакты экстренных служб -->
        <div>
          <h3 class="text-sm mb-3 font-medium text-ink">{content.disclaimer.emergencyServices.title}</h3>
          <div class="space-y-2">
            {content.disclaimer.emergencyServices.services.map((service) => (
              <div class="text-[#6B7280]">
                <p class="text-sm">{service.name}</p>
                <div class="flex flex-wrap gap-3 text-sm">
                  {service.phone && (
                    <a href={`tel:${service.phone.replace(/\s/g, '')}`} class="text-[#6B7280] hover:text-ink">
                      {service.phone}
                    </a>
                  )}
                  {service.website && (
                    <a href={`https://${service.website}`} target="_blank" rel="noopener noreferrer" class="text-[#6B7280] hover:text-ink">
                      {service.website}
                    </a>
                  )}
                </div>
              </div>
            ))}
          </div>
        </div>
        
        <div>
          <h3 class="text-sm mb-3 font-medium text-ink">{content.disclaimer.confidentiality.title}</h3>
          <p class="leading-relaxed text-[#6B7280]">{content.disclaimer.confidentiality.content}</p>
        </div>
        <div class="pt-6 border-t border-[#E5E7EB] text-center">
          <p class="text-[#6B7280]">&copy; {new Date().getFullYear()} Спиридонова Ирина. Все права защищены.</p>
        </div>
      </div>
    </div>
  </footer>

  <!-- Модальное окно с тестом -->
  <ScreeningModal />
</Layout>

<style is:global>
  .screening-trigger-btn {
    display: inline-flex;
    align-items: center;
    gap: 0.75rem;
    padding: 0.875rem 1.5rem;
    background: linear-gradient(135deg, #f5f3ff 0%, #faf8ff 100%);
    border: 2px solid #e9d5ff;
    border-radius: 16px;
    color: #7c3aed;
    font-weight: 500;
    font-size: 1rem;
    cursor: pointer;
    transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
    box-shadow: 0 2px 8px rgba(124, 58, 237, 0.1);
  }

  .screening-trigger-btn:hover {
    background: linear-gradient(135deg, #f5f3ff 0%, #ede9fe 100%);
    border-color: #c4b5fd;
    transform: translateY(-2px);
    box-shadow: 0 4px 16px rgba(124, 58, 237, 0.2);
  }

  .screening-trigger-btn svg {
    flex-shrink: 0;
  }

  .screening-badge {
    display: inline-block;
    padding: 0.25rem 0.75rem;
    background: white;
    border: 1px solid #e9d5ff;
    border-radius: 999px;
    font-size: 0.875rem;
    font-weight: 600;
    color: #7c3aed;
  }

  @media (max-width: 640px) {
    .screening-trigger-btn {
      width: 100%;
      justify-content: center;
      font-size: 0.9375rem;
    }
  }
</style>
